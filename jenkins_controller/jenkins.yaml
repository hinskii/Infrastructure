jenkins:
  authorizationStrategy:
    globalMatrix:
      entries:
      - user:
          name: "panda"
          permissions:
          - "Overall/Administer"
  securityRealm:
      local:
        users:
         - id: "panda"
           password: "panda"







